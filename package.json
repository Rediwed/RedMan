{
  "name": "redman-release",
  "version": "1.1.2",
  "private": true,
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "test": "npm --prefix app test",
    "audit": "npm --prefix app audit --audit-level=low",
    "release:check": "./scripts/release.sh check"
  }
}
