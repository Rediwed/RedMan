{
  "name": "redman",
  "version": "1.1.9",
  "private": true,
  "description": "RedMan — Homelab management tool for backups, sync and containers",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "test": "npm run test:bridge && npm run test:host && npm run test:mitigations",
    "test:bridge": "node ../test/test_upgrade_readiness.mjs && node ../test/test_upgrade_bridge_runtime.mjs && node ../test/test_backward_compat.mjs --skip-live",
    "test:host": "bash ../test/test_prepare_upgrade_host.sh",
    "lint": "eslint backend/src backend/scripts frontend/src frontend/vite.config.js",
    "test:browser": "playwright test",
    "test:greenfield": "bash ../test/test_greenfield_backup_account.sh && bash ../test/test_greenfield_compose.sh && bash ../test/test_docker_proxy_acl.sh",
    "test:mitigations": "node ../test/test_mitigation_regressions.mjs",
    "validate": "bash ../scripts/validate.sh",
    "seed": "npm run seed -w backend",
    "reseed": "npm run seed && echo '\nRe-seeded. Restart the server to pick up changes.'",
    "stop": "lsof -ti:8090,5173,5174,5175 | xargs kill -9 2>/dev/null && echo 'Servers stopped.' || echo 'No servers running.'",
    "restart": "npm run stop && npm run dev",
    "clean": "rm -f backend/data/*.db backend/data/*.db-wal backend/data/*.db-shm && echo 'Database removed.'",
    "fresh": "npm run clean && npm run seed && echo 'Fresh database ready.'"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "concurrently": "9.2.4",
    "eslint": "^9.39.5",
    "globals": "^15.15.0"
  },
  "overrides": {
    "@grpc/grpc-js": "1.14.4",
    "@protobufjs/utf8": "1.1.2",
    "path-to-regexp": "0.1.13",
    "picomatch": "4.0.5",
    "protobufjs": "7.6.5",
    "shell-quote": "1.9.0"
  }
}
